'use client';

import { DataTable, type Column } from '../_components/data-table'; import { PageHeading } from '../_components/page-heading'; import { users } from '../_components/records';
const columns: Column<(typeof users)[number]>[] = [{ label: 'Customer', render: (row) => <div><p className="font-semibold">{row.name}</p><p className="mt-1 text-xs text-ink/50 dark:text-white/50">{row.email}</p></div> }, { label: 'Orders', render: (row) => row.orders }, { label: 'Lifetime value', render: (row) => `$${row.spent.toLocaleString()}` }, { label: 'Status', render: (row) => <span className={`rounded-full px-2.5 py-1 text-xs ${row.status === 'Active' ? 'bg-emerald-100 text-emerald-800' : 'bg-stone-200 text-stone-700'}`}>{row.status}</span> }];
export default function UsersPage() { return <div><PageHeading title="Customers" description="Understand the people behind your orders and protect valuable relationships." /><div className="mt-8 grid gap-4 sm:grid-cols-3"><section className="border border-ink/10 bg-white p-5 dark:border-white/10 dark:bg-white/[.03]"><p className="text-xs uppercase tracking-widest text-ink/50 dark:text-white/50">All customers</p><p className="mt-2 font-serif text-3xl">1,284</p></section><section className="border border-ink/10 bg-white p-5 dark:border-white/10 dark:bg-white/[.03]"><p className="text-xs uppercase tracking-widest text-ink/50 dark:text-white/50">Returning</p><p className="mt-2 font-serif text-3xl">62.4%</p></section><section className="border border-ink/10 bg-white p-5 dark:border-white/10 dark:bg-white/[.03]"><p className="text-xs uppercase tracking-widest text-ink/50 dark:text-white/50">New this month</p><p className="mt-2 font-serif text-3xl">186</p></section></div><div className="mt-7"><DataTable columns={columns} rows={users} /></div></div>; }
