'use client';
import { useState } from 'react'; import { X } from 'lucide-react';
export function DrawerForm({ title, children, trigger }: { title: string; children: React.ReactNode; trigger: React.ReactNode }) { const [open, setOpen] = useState(false); return <><span onClick={() => setOpen(true)}>{trigger}</span>{open && <div className="fixed inset-0 z-[80] bg-black/50" onClick={() => setOpen(false)}><section onClick={(event) => event.stopPropagation()} className="ml-auto flex h-full w-full max-w-lg flex-col bg-[#f7f4ed] p-6 shadow-2xl dark:bg-[#171411]"><div className="flex items-center justify-between"><h2 className="font-serif text-3xl">{title}</h2><button onClick={() => setOpen(false)}><X /></button></div><div className="gold-line my-6" />{children}</section></div>}</>; }
export function Field({ label, children }: { label: string; children: React.ReactNode }) { return <label className="block text-xs font-bold uppercase tracking-[.12em] text-ink/65 dark:text-white/65"><span>{label}</span><div className="mt-2">{children}</div></label>; }
export const inputClass = 'w-full border border-ink/15 bg-white px-3 py-2.5 text-sm font-normal normal-case tracking-normal outline-none focus:border-gold dark:border-white/15 dark:bg-white/5';
