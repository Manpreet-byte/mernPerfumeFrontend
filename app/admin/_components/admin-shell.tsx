'use client';
import Link from 'next/link'; import { usePathname } from 'next/navigation'; import { useState } from 'react'; import { BarChart3, Boxes, ClipboardList, Menu, MessageSquareText, Moon, Search, Settings, Sun, Tags, TicketPercent, Users, X } from 'lucide-react'; import { useTheme } from 'next-themes';
const navigation = [{ label: 'Dashboard', href: '/admin', icon: BarChart3 }, { label: 'Products', href: '/admin/products', icon: Boxes }, { label: 'Categories', href: '/admin/categories', icon: Tags }, { label: 'Orders', href: '/admin/orders', icon: ClipboardList }, { label: 'Coupons', href: '/admin/coupons', icon: TicketPercent }, { label: 'Users', href: '/admin/users', icon: Users }, { label: 'Reviews', href: '/admin/reviews', icon: MessageSquareText }, { label: 'Settings', href: '/admin/settings', icon: Settings }];
export function AdminShell({ children }: { children: React.ReactNode }) { const pathname = usePathname(); const [open, setOpen] = useState(false); const { theme, setTheme } = useTheme(); const sidebar = <aside className="flex h-full w-72 flex-col bg-[#12100d] px-5 py-7 text-stone-300"><Link href="/admin" className="px-3 font-serif text-2xl tracking-[.22em] text-white">AURELIA</Link><p className="mt-2 px-3 text-[10px] font-bold uppercase tracking-[.26em] text-gold">Admin atelier</p><nav className="mt-11 space-y-1">{navigation.map(({ label, href, icon: Icon }) => <Link onClick={() => setOpen(false)} href={href} key={href} className={`flex items-center gap-3 px-3 py-3 text-sm transition ${pathname === href ? 'bg-gold text-white' : 'hover:bg-white/10 hover:text-white'}`}><Icon size={18} />{label}</Link>)}</nav><div className="mt-auto border-t border-white/10 px-3 pt-5 text-xs leading-5 text-stone-500">Aurelia commerce<br />Management suite</div></aside>; return <div className="min-h-screen bg-[#f7f4ed] text-ink dark:bg-[#171411] dark:text-stone-100"><div className="fixed inset-y-0 left-0 z-[60] hidden lg:block">{sidebar}</div>{open && <div className="fixed inset-0 z-[70] bg-black/50 lg:hidden" onClick={() => setOpen(false)}><div className="h-full w-72" onClick={(event) => event.stopPropagation()}>{sidebar}</div></div>}<div className="lg:pl-72"><header className="sticky top-0 z-40 flex h-[72px] items-center justify-between border-b border-ink/10 bg-[#f7f4ed]/95 px-5 backdrop-blur dark:border-white/10 dark:bg-[#171411]/95 sm:px-8"><div className="flex items-center gap-4"><button className="lg:hidden" onClick={() => setOpen(!open)} aria-label="Open navigation">{open ? <X /> : <Menu />}</button><div className="hidden items-center gap-2 border border-ink/15 bg-white px-3 py-2 dark:border-white/15 dark:bg-white/5 sm:flex"><Search size={16} className="text-gold" /><input className="w-48 bg-transparent text-sm outline-none" placeholder="Search records" /></div></div><div className="flex items-center gap-4"><button onClick={() => setTheme(theme === 'dark' ? 'light' : 'dark')} className="rounded-full border border-ink/15 p-2 dark:border-white/15" aria-label="Toggle theme">{theme === 'dark' ? <Sun size={16} /> : <Moon size={16} />}</button><div className="hidden text-right sm:block"><p className="text-sm font-semibold">Aurelia Admin</p><p className="text-xs text-ink/50 dark:text-white/50">Administrator</p></div><div className="grid h-9 w-9 place-items-center rounded-full bg-gold text-xs font-bold text-white">AA</div></div></header><main className="px-5 py-8 sm:px-8 lg:px-10">{children}</main></div></div>; }
