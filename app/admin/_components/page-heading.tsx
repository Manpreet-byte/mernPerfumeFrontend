import type { ReactNode } from 'react'; export function PageHeading({ eyebrow = 'Aurelia administration', title, description, action }: { eyebrow?: string; title: string; description: string; action?: ReactNode }) { return <div className="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><p className="eyebrow">{eyebrow}</p><h1 className="mt-2 font-serif text-4xl sm:text-5xl">{title}</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-ink/60 dark:text-white/60">{description}</p></div>{action}</div>; }
